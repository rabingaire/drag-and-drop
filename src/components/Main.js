import React from 'react';

import { Form } from './common';

export default class Main extends React.Component {
    render(){
        return (
            <div>
                <Form />
            </div>
        )
    }
}